# Development-only dependencies.
#
# Pillow is needed ONLY to (re)generate the pixel-art sprite PNGs with
# `python sprites.py`. The bot never imports Pillow at runtime — it just
# attaches the pre-baked files in sprites/. Install with:
#   pip install -r requirements-dev.txt
Pillow>=10.0.0
