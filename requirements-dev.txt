# Development-only dependencies.
#
# Pillow + numpy are needed ONLY to (re)generate the animated glow sprites with
# `python sprites.py`. The bot never imports them at runtime — it just attaches
# the pre-baked GIFs in sprites/. Install with:
#   pip install -r requirements-dev.txt
Pillow>=10.0.0
numpy>=1.24.0
