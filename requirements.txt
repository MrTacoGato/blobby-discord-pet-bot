discord.py>=2.3.0
boto3>=1.34.0
python-dotenv>=1.0.0
# Compositing equipped cosmetics onto the pet at runtime (Phase 2). Each
# dressed look is generated once and cached, so this stays light at runtime.
Pillow>=10.0.0
numpy>=1.24.0
